export function parseUrlParametersForSetState({ urlParams }) {
  const { id } = urlParams;

  return { <%= o.key %>: id };
}

// eslint-disable-next-line no-unused-vars
export function checkNeedUpdateAssist(
  currentState,
  preProperties,
  preState,
  // eslint-disable-next-line no-unused-vars
  snapshot,
) {
  const { <%= o.key %> } = currentState;

  const { <%= o.key %>: <%= o.key %>Pre } = preState;

  return <%= o.key %>Pre !== <%= o.key %>;
}
